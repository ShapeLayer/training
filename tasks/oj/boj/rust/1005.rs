use std::io;

fn main() {
  let mut puts = String::new();
  io::stdin().read_line(&mut puts).unwrap();
  let t: i32 = puts.trim().parse::<i32>().unwrap();
  for i in 0..t {
    let mut puts = String::new();
    io::stdin().read_line(&mut puts).unwrap();
    let (n, k) = {
    }
  }
}